# Base URL for the Campus Connect API
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api/v1

# Example auth token loader (optional) - configure when implementing auth storage
# NEXT_PUBLIC_MOCK_TOKEN=your-jwt-token
